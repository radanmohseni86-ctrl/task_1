# Copy to .env and fill in real values for local development.
# .env is gitignored — never commit real credentials.

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=task_1_hamrah
DB_SSLMODE=disable

REDIS_ADDR=localhost:6379
LOGGER_ADDR=localhost:9090

GRPC_PORT=8080
